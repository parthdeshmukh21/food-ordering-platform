apiVersion: v1
kind: Service

metadata:
  name: notification-service
  namespace: food-ordering

spec:
  selector:
    app: notification-service

  ports:
  - port: 80
    targetPort: 8080

  type: ClusterIP
